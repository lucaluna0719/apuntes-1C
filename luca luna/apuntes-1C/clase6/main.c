#include <stdio.h>
#include <stdlib.h>
void ordenarValor(int[], int);

int main()
{
    int valores[10]={5,7,1,9,6,4,3,7,10,0};
    ordenarVector

    return 0;
}
void ordenarValor(int numeros[], int tam);
{
    int i, j;
    for(i=0;i<tam-1; i++)
    {
        for(j=i+1; j<tam; j++)
        {
            printf("Para i=%d, con j=%d"numero[i],numero[i])
            if(numero[i]> numeroos[j])//criterio
            {
                aux = -numeros[i];
                numeros[i]=numeros[j];
                numeros[j]= aux;
            }
        }
    }


}
